services:
  - type: web
    name: audit-angel
    runtime: node
    plan: free
    region: ohio
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.22.0
      - key: DATABASE_URL
        fromDatabase:
          name: audit-angel-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: DEFAULT_USER_EMAIL
        sync: false
      - key: DEFAULT_USER_PASSWORD
        sync: false

databases:
  - name: audit-angel-db
    plan: free
    region: ohio
    databaseName: audit_angel
    user: audit_angel
